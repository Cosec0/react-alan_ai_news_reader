import { makeStyles } from '@mui/styles';

export default makeStyles({
    header: { 
        width: '100%', 
        display: 'flex', 
        flexDirection: 'column', 
        alignItems: 'center', 
        marginBottom: '2rem'
    }
});